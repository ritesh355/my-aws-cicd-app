version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo Build completed
artifacts:
  files:
    - '**/*'
  discard-paths: no
  exclude:
    - node_modules/**
    - package-lock.json
